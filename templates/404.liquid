<!-- 404 Page Not Found -->
<div class="err404">
  <div class="err404-inner">
    <div class="err404-eyebrow">
      <span class="err404-eyebrow-line"></span>
      <span>Error 404</span>
      <span class="err404-eyebrow-line"></span>
    </div>

    <p class="err404-code">404</p>
    <h1 class="err404-title">This Page Has Moved On</h1>
    <p class="err404-text">
      The page you&rsquo;re looking for may have been relocated or retired &mdash; but our
      collection of the world&rsquo;s finest humidors is still right here, waiting.
    </p>

    <form action="{{ routes.search_url }}" method="get" role="search" class="err404-search">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
      <input type="search" name="q" placeholder="Search humidors, accessories, guides..." aria-label="Search" />
      <button type="submit">Search</button>
    </form>

    <div class="err404-ctas">
      <a href="/collections/all" class="btn btn-primary">Browse All Humidors</a>
      <a href="/" class="btn btn-secondary">Back to Home</a>
    </div>

    <div class="err404-links">
      <span class="err404-links-label">Popular destinations</span>
      <nav class="err404-links-grid">
        <a href="/collections/cabinet-humidors">Cabinet Humidors</a>
        <a href="/collections/desktop-humidors">Desktop Humidors</a>
        <a href="/collections/electronic-humidors">Electronic Humidors</a>
        <a href="/collections/travel-humidors">Travel Humidors</a>
        <a href="/collections/new-arrivals">New Arrivals</a>
        <a href="/pages/walk-in-humidor">Bespoke Humidors</a>
        <a href="/blogs/news">The Journal</a>
        <a href="/pages/about-us">About Dunn's</a>
      </nav>
    </div>

    <p class="err404-help">
      Need a hand? Call <a href="tel:8884319214">(888) 431-9214</a> or email
      <a href="mailto:support@dunnluxuryselections.com">support@dunnluxuryselections.com</a>.
    </p>
  </div>
</div>

<style>
  .err404 {
    background: var(--charcoal-950);
    padding: 4rem 1.5rem 5rem;
  }
  .err404-inner {
    max-width: 42rem;
    margin: 0 auto;
    text-align: center;
  }
  .err404-eyebrow {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    color: var(--gold-400);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
  }
  .err404-eyebrow-line { width: 2rem; height: 1px; background: var(--gold-500); }

  .err404-code {
    font-family: 'Playfair Display', Georgia, serif;
    font-weight: 700;
    font-size: clamp(4.5rem, 16vw, 8rem);
    line-height: 1;
    margin: 0;
    background: linear-gradient(135deg, var(--gold-500) 0%, var(--gold-200) 50%, var(--gold-600) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .err404-title {
    font-family: 'Playfair Display', Georgia, serif;
    color: #fff;
    font-size: clamp(1.6rem, 4vw, 2.25rem);
    font-weight: 700;
    margin: 0.75rem 0 1rem;
  }
  .err404-text {
    color: rgba(240, 221, 184, 0.7);
    font-size: 1rem;
    line-height: 1.7;
    max-width: 34rem;
    margin: 0 auto 2rem;
  }

  .err404-search {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    max-width: 30rem;
    margin: 0 auto 2rem;
    padding: 0.35rem 0.35rem 0.35rem 0.9rem;
    background: var(--charcoal-900);
    border: 1px solid rgba(201, 138, 16, 0.3);
    border-radius: 0.6rem;
  }
  .err404-search svg { color: rgba(240, 221, 184, 0.45); flex-shrink: 0; }
  .err404-search input {
    flex: 1;
    min-width: 0;
    background: transparent;
    border: none;
    outline: none;
    color: var(--cream-100);
    font-size: 0.9rem;
  }
  .err404-search input::placeholder { color: rgba(240, 221, 184, 0.4); }
  .err404-search button {
    flex-shrink: 0;
    padding: 0.6rem 1.1rem;
    border: none;
    border-radius: 0.4rem;
    background: linear-gradient(135deg, var(--gold-500) 0%, var(--gold-600) 50%, var(--gold-700) 100%);
    color: var(--charcoal-950);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    cursor: pointer;
  }

  .err404-ctas {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 3rem;
  }

  .err404-links { border-top: 1px solid rgba(70, 62, 59, 0.5); padding-top: 2rem; }
  .err404-links-label {
    display: block;
    color: rgba(240, 221, 184, 0.4);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }
  .err404-links-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem 0.75rem;
  }
  .err404-links-grid a {
    padding: 0.5rem 1rem;
    border: 1px solid rgba(70, 62, 59, 0.6);
    border-radius: 9999px;
    color: rgba(240, 221, 184, 0.8);
    font-size: 0.82rem;
    text-decoration: none;
    transition: color 0.2s ease, border-color 0.2s ease, background 0.2s ease;
  }
  .err404-links-grid a:hover {
    color: var(--gold-400);
    border-color: rgba(201, 138, 16, 0.5);
    background: rgba(201, 138, 16, 0.06);
  }

  .err404-help {
    margin: 2.5rem 0 0;
    font-size: 0.85rem;
    color: rgba(240, 221, 184, 0.55);
  }
  .err404-help a { color: var(--gold-400); text-decoration: none; }
  .err404-help a:hover { text-decoration: underline; }
</style>
